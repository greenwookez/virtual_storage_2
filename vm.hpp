#include <vector>
using namespace std;

const uint64_t PROCESS_DEFAULT_PROGRAM_SIZE;
const uint64_t PROCESS_DEFAULT_REQUESTED_MEMORY;


typedef uint64_t PageNumber;
typedef PageNumber VirtualAddress;
typedef PageNumber RealAddress;
